name: Publish PDF to GitHub Pages
on:
  push:
    branches: [ "main" ]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Fonts
        run: |
          sudo apt-get install -y fonts-noto-cjk fonts-lmodern
          fc-cache -f

      - uses: typst-community/setup-typst@v4

      - name: Create output directory
        run: mkdir -p github-pages-temp

      - name: Build PDF
        run: typst compile main.typ github-pages-temp/document.pdf

      # 👇 修改后的 index.html，自动跳转 PDF
      - name: Generate index.html with redirect
        run: |
          cat > github-pages-temp/index.html << 'EOF'
          <!DOCTYPE html>
          <html>
          <head>
              <meta charset="UTF-8">
              <title>Redirecting to PDF...</title>
              <meta http-equiv="refresh" content="0; url=document.pdf">
          </head>
          <body>
              <p>If you are not redirected automatically, <a href="document.pdf">click here</a>.</p>
          </body>
          </html>
          EOF

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: github-pages-temp
        
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
