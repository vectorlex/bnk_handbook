name: Publish PDF to GitHub Pages
on:
  push:
    branches: [ "main" ]

# 设置 Pages 所需的权限
permissions:
  contents: read
  pages: write
  id-token: write

# 确保只有一个部署任务在运行
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: typst-community/setup-typst@v4

      - name: Create output directory
        run: mkdir -p github-pages-temp

      - name: Build PDF
        run: typst compile main.typ github-pages-temp/document.pdf

      # 👇 新增步骤：生成 index.html
      - name: Generate index.html
        run: |
          cat > github-pages-temp/index.html << 'EOF'
          <!DOCTYPE html>
          <html>
          <head>
            <meta charset="UTF-8">
            <title>我的 Typst 文档</title>
          </head>
          <body>
            <h1>编译好的 PDF 文档</h1>
            <p><a href="document.pdf" target="_blank">点击查看 document.pdf</a></p>
            <p>或者直接嵌入 PDF（需要浏览器支持）：</p>
            <embed src="document.pdf" type="application/pdf" width="100%" height="600px" />
          </body>
          </html>
          EOF

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: github-pages-temp
        
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
